const CONFIG = {
  name: "BookStop",
  API_URI: process.env.API_URI || "http://localhost:8080",
  APP_URI: process.env.APP_URI || "http://localhost:3000",
  facebookURL: "BookStopProject",
  twitterURL: undefined,
  youtubeURL: undefined,
};

export default CONFIG;
